<% layout( 'layout-scenes' ) -%>
 hello
<% items.forEach( function ( person ){ %>
  <p>
    <span>
      <a href="/persons/edit/<%= item._id %>" title="Update this person item"><%= item.shortname %></a>
      
      <%= item.name %>
      <%= item.surename %>
      <%= item.birth %>
      <%= item.birth_place %>
      <%= item.death %>
      <%= item.death_place %>
      <%= item.profession %>
      <%= item.bio %>
      
      <% item.images.forEach( function ( image ){ %>
      	<img src="<%= image %>" />
      <% }); %>
      <br/>
      <br/>
    </span>
    <span>
      <!--<a href="/persons/destroy/<%= person._id %>" title="Delete this person item">Delete</a>-->
    </span>
  </p>
<% }); %>
