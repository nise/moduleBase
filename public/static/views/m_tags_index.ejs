<% layout( 'm_layout' ) -%>

<div  class="jumbotron">
	<div class="container">
  <h2>Schlüsselwörter</h2>
  <% if(user !== undefined){ %>
  <!--<a href="/tags/new" class="label label-primary"><span class="glyphicon glyphicon-plus"></span> Create new</a>-->
  <% } %>
  </div>
</div>
<div class="container">
	
<% for(var t in tags ){ if(tags.hasOwnProperty(t)){%>
		<label><span class="glyphicon glyphicon-null" aria-hidden="true"></span> <%= t %> <a href="/admin/tags/edit/<%=: t | replace:/\ /g,'-' %>"> <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> </a></label> <br>
		<% for(var m in tags[t].modules ){ if(tags[t].modules.hasOwnProperty(m)){%>
			<a href="/modules/view/<%= tags[t].modules[m]._id %>"><%= tags[t].modules[m].title %></a>,
		<% }}; %>
		<br><br>
	<% }} %>
</div>	

